对象适配器

    --> 对象适配器则是 引用 关系。

    角色包含

        --> 目标接口(Target)

            --> 客户所期待的接口，可以是具体的或抽象的类，也可以是接口

        --> 需要适配的类(AdapterClass)

            --> 需要适配的类或适配者类；

        --> 适配器(Adapter)

            --> 通过包装一个需要适配的对象，把原接口转换成目标接口


    在适配类中传入需要转换的对象，完成转换而已。